import { createTheme, Theme } from '@mui/material';
import { palette } from './palette';
import { components } from './components';

export const lightTheme: Theme = createTheme({ palette, components });
