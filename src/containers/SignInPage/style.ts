import { SxProps, Theme } from '@mui/material';

interface SignInPageStyle {
  container: SxProps<Theme>;
}

export const styles: SignInPageStyle = {
  container: {
    display: 'flex',
    flex: 1,
  },
};
